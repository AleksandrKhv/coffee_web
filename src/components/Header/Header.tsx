import React from 'react';
import st from './Header.module.css'

const Header = () => {
    return (
        <header>
            <a href="#" className={st.logo}><i></i></a>
        </header>
    );
};

export default Header;